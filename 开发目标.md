# 枝图：开发目标

更新时间：2026-09-10
阶段：已迁移为 0.2 原生 macOS 开发版；尚未完成全部范围验收。当前能力和未完成项见 [README.md](README.md)。

## 1. 项目目标

开发一款自用的思维导图软件，替代用户日常使用的付费 MindNode，减少订阅支出。围绕真实的项目规划场景，优先做好节点编辑、分支布局、键盘操作和本地数据保存，让用户能够长期使用自己的工具管理思路与工作进展。

本项目是独立软件开发。连接或自动化操作现有 MindNode，不是本项目的交付目标。

## 2. 依据与决策状态

- **用户明确确认**：开发自己的 MindNode 替代品；保留下表所有标记为“保留”的功能，包括 MCP 和插入图片。取消内置 AI、云同步、多人协作、多端、LaTeX、贴纸和空间计算。这些取消项不再作为后续待开发目标。
- **图片硬性要求**：工程内的用户图片统一为 JPG；允许选择或拖入 JPG/JPEG、PNG，PNG 必须自动转换为 JPG 后存入画布工程。
- **导图导出硬性要求**：整张导图仅提供 JPG、PDF 两种视觉导出格式，不提供 PNG、SVG 导出。Markdown、OPML 的结构化数据交换功能保留。
- **使用场景**：通过多级分支组织项目阶段、素材清单、工作事项，并在节点上记录进展。公开仓库只包含通用示例和测试素材。
- **官方调研事实**：下文记录已阅读的 MindNode 官方能力，并映射为本项目的功能目标；不意味着所有功能都属于首版。
- **产品边界**：面向用户的 Mac，单人、本地使用，不做云同步、多人协作和多端支持。用户已确认只做 Mac，采用 Swift + AppKit 原生应用；界面、画布、编辑和桌面存储均不使用 WebView / HTML / JavaScript。
- **尚未验证**：MindNode 原生文件兼容程度、各交换格式的信息保留范围，以及具体布局算法和大图性能。

## 3. 核心使用场景

1. 创建项目根节点，将项目持续拆成阶段、事项和具体工作。
2. 主要通过键盘输入和扩展分支，用鼠标移动、选中、调整层级。
3. 在大画布中查看整体结构，折叠暂不处理的分支，聚焦当前工作。
4. 通过节点文字与颜色识别事项及状态，随后扩展为任务勾选和进度汇总。
5. 随时关闭并再次打开应用，继续编辑原有内容和结构。
6. 通过开放格式迁入现有资料，并导出给其他软件或用于分享。

## 4. 官方特性到本项目目标的映射

下表是用户本次筛选后的完整开发范围。P0/P1/P2 仅表示实施顺序；所有“保留”项均属于最终交付范围，不能因排在后面而默认省略。

| 官方能力 | 本项目开发目标 | 范围与顺序 |
| --- | --- | --- |
| 无限画布与层级导图 | 平移、缩放、节点编辑、分支结构、自动布局、快捷键、撤销重做 | 保留，P0 |
| 节点与分支样式 | 深色主题、自定义颜色、字体、节点形状和分支样式 | 保留，P0/P1 |
| 折叠分支 | 折叠和展开子树，保留内容与结构 | 保留，P0 |
| 本地文档 | 新建、打开、自动保存、重开恢复和备份恢复 | 保留，P0 |
| 导入与导出 | Markdown、OPML 导入导出；整张导图导出 JPG、PDF | 保留，P0 |
| MindNode 原生导入 | 支持迁入现有 `.mindnode` 文件；按实测明确支持版本和字段 | 保留，P1 |
| 导图与大纲同步 | 同一数据模型的导图、大纲两种编辑视图 | 保留，P1 |
| 备注 | 节点附加长说明 | 保留，P1 |
| 图片 | 选择或拖入 JPG/JPEG、PNG；工程内统一保存为 JPG | 保留，P1 |
| 标签、筛选、聚焦 | 跨分支归类、筛选、高亮、聚焦当前子树 | 保留，P1 |
| 任务与汇总进度 | 勾选任务，按子任务汇总父节点进度 | 保留，P1 |
| 非层级连接线 | 连接不同分支节点，添加关系标题 | 保留，P1 |
| 多种布局与主题 | 多种布局、手动摆放节点和主题预设 | 保留，P1 |
| 文档与节点深链接 | 文档间跳转，定位到指定节点 | 保留，P2 |
| Apple 快捷指令 | 通过本地接口操作文档和节点 | 保留，P2 |
| MCP | 提供本地结构化读写接口，供外部客户端读取、创建和修改导图 | 保留，P2 |
| 内置 AI | 不集成模型、聊天、AI 生成或 AI 整理功能；MCP 独立保留 | 取消 |
| 云同步、多人协作、多端 | 不开发相关功能 | 取消 |
| LaTeX、贴纸、空间计算 | 不开发相关功能 | 取消 |

## 5. P0：首版交付范围

### 画布与布局

- 可平移、缩放的画布，支持回到中心和适配全部内容。
- 优先实现截图所示的水平树形布局、曲线分支与深色主题。
- 节点增删、文字变化、子树折叠或移动后自动重新布局，避免节点重叠。
- 同一画布允许多个独立主节点；空白处右键新建，各棵树分别移动、保存并完整导出。
- 节点文字支持中文和多行显示。

### 编辑交互

- 新建文档，编辑文档名称与节点文字。
- 新增同级节点、子节点，删除节点及其子树。
- 拖动调整父子关系与同级顺序，阻止将节点移入其自身后代。
- 键盘快速新增、编辑、删除、导航；提供可发现的快捷键说明。
- 撤销与重做覆盖内容编辑、结构调整、删除及样式修改。
- 修改节点、分支颜色及基础外观。

### 本地数据

- 使用带格式版本号的可导出文档模型；节点拥有稳定 ID。
- 本地自动保存，显示保存状态与失败提示；提供手动保存或导出入口。
- 重开应用后恢复文档文字、层级、顺序、样式与折叠状态。
- 保存过程避免损坏已有文件，保留可恢复的上一份有效数据。
- 首版核心功能离线可用，无需购买 MindNode 或调用付费 AI 接口。

### 文件互通

- Markdown、OPML 导入导出至少保留节点标题、层级与同级顺序。
- 导出当前导图为 JPG、PDF，输出应覆盖完整目标内容，避免截断；不提供 PNG、SVG 导出。
- 明确每种交换格式支持和不支持的字段；不能用 Markdown 或 OPML 替代完整原生备份。
- 先使用测试文件验证导入；保留用户原始资料，不覆盖原文件。
- `.mindnode` 原生文件导入属于 P1 必做项；先以实际样本确认格式，按支持版本验收，遇到无法迁入的字段需明确报告，不能静默丢弃。尚未验证前不声称完整兼容。

## 6. 图片与 MCP 的专项要求

### 图片进入工程的规则

- 文件选择与拖放入口统一支持 JPG/JPEG 和 PNG；工程内用户图片资产仅保存为 `.jpg`，实际编码必须是 JPEG，不能只改扩展名。
- JPG/JPEG 校验后复制进工程；PNG 解码、转换为 JPG 成功后再插入节点。转换失败应提示原因，不创建损坏的图片引用。
- 工程自带图片资产，不依赖外部原图路径；移动工程或移走原图后仍能显示图片。
- 不修改或删除用户原图，不把输入 PNG 副本留作工程图片资产；原生文件迁入的图片同样遵守 JPG 存储规则。
- 其他图片格式当前不接受，提供明确提示；校验实际文件类型，不只看扩展名。
- **实现默认值（可调整）**：PNG 透明区域转为白底，保留原始像素尺寸，使用高质量 JPEG 编码。JPG 不支持透明度，因此转换结果不保留透明通道。
- 图片支持随节点显示、调整显示尺寸、替换和删除；重开、撤销重做与工程导出后引用保持正确。
- 整张导图仅支持 JPG、PDF 视觉导出；PNG 仅作为可自动转为 JPG 的导入格式，不作为工程图片资产或导出格式。

### MCP

- 为本软件提供本地 MCP 接口，支持列出和读取文档、创建文档、读取与增删改节点及调整层级。
- MCP 与界面操作使用同一文档模型、校验和保存机制；变更可在界面正确呈现，不能形成两份不一致的数据。
- 通过 MCP 导入图片时，同样遵守图片格式与转换规则。
- 提供连接说明和读写权限控制，使用真实客户端验证读写流程。
- 不在应用内增加 AI 聊天入口、模型配置或模型调用；外部客户端可通过 MCP 使用本软件。

## 7. 验收标准

### P0 首版

1. 能从空白文档连续创建不少于五层的中文项目树，完成同级、子级新增及文字编辑。
2. 移动一整棵子树后，其内部层级和内容保持完整；无效的循环移动被阻止。
3. 删除子树后撤销可完整恢复；重做结果正确。
4. 长文本、折叠、展开、拖动之后布局无明显重叠，画布导航可用。
5. 保存并关闭应用后重开，内容、结构和已支持的视觉设置一致。
6. 保存失败有明确提示；恢复机制可通过模拟中断或损坏测试验证。
7. Markdown、OPML 使用代表性中文样例做往返测试，已声明支持的结构信息一致。
8. JPG、PDF 导出可正常打开，中文、图片和连线显示正确，无意外裁切；视觉导出选项中不存在 PNG、SVG。
9. 用至少 500 个节点的测试文档验证打开、编辑、缩放与折叠，记录实际性能；出现明显卡顿时优化。该规模是初步测试基线，不代表已经通过验证。
10. 用户能够在不使用付费 MindNode 的情况下完成“建立项目 → 拆分工作 → 调整结构 → 保存 → 重开 → 导出”的完整流程。

### 完整范围补充验收

1. JPG、JPEG、普通 PNG、含透明区域的 PNG 均验证选择导入及拖放；工程图片实际编码均为 JPEG，且不残留 PNG 图片资产。透明 PNG 按已声明的背景规则转换。
2. 移走原图并重开工程，图片仍正常显示；替换、删除、撤销重做后图片引用有效。
3. 不支持的图片或损坏文件明确报错，既有工程保持完好。
4. 导图与大纲双向编辑一致；备注、图片、标签、任务、关系线、样式、布局和跨文档链接保存后可恢复。
5. 使用真实 `.mindnode` 样本验证已声明支持的结构、字段与图片迁入，对不支持项给出提示。
6. MCP 客户端完成读取、创建、修改、层级调整和保存重开验证；界面与 MCP 所见一致，读写权限有效。
7. Apple 快捷指令完成文档或节点的真实操作验证。
8. 所有标记“保留”的功能均完成对应验收，才算完成整个项目。

## 8. 实施顺序与完成定义

1. **最小可运行版**：文档模型、画布、节点编辑、树形布局。
2. **可日常使用的首版**：补齐结构操作、撤销重做、样式、可靠保存、导入导出，逐项通过 P0 验收。
3. **日常增强版**：实现大纲、备注、JPG 图片流程、标签与筛选、任务、关系线、多种布局和主题、原生文件迁入。
4. **自动化扩展版**：完成跨文档链接、MCP 和 Apple 快捷指令；不集成内置 AI。

完成标准是可实际安装或运行、可持续编辑、可恢复数据的应用。只有静态界面、不能编辑的导图展示或没有可靠保存的原型，都不算完成首版。

## 9. 已阅读的官方资料

以下资料作为早期功能规划参考；当前实现与未完成项以 README 为准。

- [支持入口](https://www.mindnode.com/support)
- [功能总览](https://www.mindnode.com/features)
- [大纲视图](https://www.mindnode.com/support/guides/outlining)
- [跨节点连接](https://www.mindnode.com/support/guides/connections)
- [任务与提醒事项同步](https://www.mindnode.com/support/guides/tasks)
- [导入与导出](https://www.mindnode.com/support/guides/import-and-export)
- [Apple 快捷指令](https://www.mindnode.com/support/guides/apple-shortcuts)
- [MCP 与 AI 助手](https://www.mindnode.com/support/guides/mcp)
- [实时协作](https://www.mindnode.com/support/guides/live-collaboration)
- [新版 MindNode 的文档管理与版本兼容](https://www.mindnode.com/support/guides/mindnode-next)
- [MindNode Classic](https://www.mindnode.com/support/guides/mindnode-classic)
- [Classic 功能迁移路线图](https://www.mindnode.com/support/guides/features-classic-next)
- [免费编辑器与 Plus 订阅](https://www.mindnode.com/support/guides/mindnode-plus)

调研中的相关边界：新版与 Classic 无法直接同步，互相导入也可能有功能差异；提醒事项中新建的任务目前不会回流到 MindNode；官方 MCP 默认只读且需显式开启编辑权限。这些是参考产品的事实，不是本项目必须照搬的限制。
