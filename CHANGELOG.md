# 更新记录

## 0.2.2 — 2026-09-10

- 空白画布左键拖动改为框选，显示半透明选择框和已选数量；支持反向框选、Shift / ⌘ 追加选择和点击切换选择。
- 拖动选中节点时一并移动所选子树；父节点和后代同时选中不会重复位移。支持批量删除和撤销，仍保留最后一个主节点。
- 按住空格拖动或使用滚轮平移画布；轻按空格仍可编辑单个选中节点。
- Esc 取消框选并恢复原选择；框选本身不移动画布，也不写入文档历史。

## 0.2.1 — 2026-09-10

首个公开的 macOS 原生开发版本，采用 PolyForm Noncommercial 1.0.0 非商用许可证。

- Swift + AppKit 原生窗口、画布、文本编辑、大纲和检查器。
- 空白画布右键新建主节点；同一工程包含多棵独立树，支持分别移动、撤销、保存和导出。
- 分支移动时连线和后代实时跟随；窗口拖动、双击顶部最大化 / 恢复、⌘W 关闭。
- 本地自动保存、原子写入、版本冲突检测、上一版本备份，以及文件事件驱动的 MCP 变更同步。
- JPG 图片嵌入工程；PNG 输入自动转换为 JPG；导出 JPG 和可搜索文字的 PDF。
- Markdown / OPML 结构交换；默认只读、显式启用写入的本地 MCP / CLI。

发布包：Apple Silicon arm64，部署目标 macOS 14.0；实测 macOS 26.4.1。应用采用 ad-hoc 签名，尚未 Apple 公证。

这是开发版本，尚未实现 `.mindnode` 原生导入、独立标签管理器、自定义主题保存或 App Intents 面板。完整能力与限制见 [README.md](README.md)。
